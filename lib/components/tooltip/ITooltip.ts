import { ClassNames } from "../../shared/interfaces";

export interface ITooltip extends ClassNames {
  tooltip: string;
  tooltipNorte: string;
  tootipOeste: string;
  tooltipSur: string;
  tooltipEste: string;
}
