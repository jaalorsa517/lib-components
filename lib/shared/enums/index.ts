export enum Types {
  STRING = "string",
  NUMBER = "number",
  BOOLEAN = "boolean",
  OBJECT = "object",
}
export enum Validates {
  ERROR = "error",
  VALIDATE = "validate",
}
export enum Attributes {
  hash = "j5-hash",
}
